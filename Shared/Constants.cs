namespace Shared
{
    /// <summary>
    /// Provides constant values like the authority infos and credentials.
    /// </summary>
    public static class Constants
    {
        public const string Authority = ""; // Authority url
        public const string ClientId = ""; // Client id
        public const string ClientSecret = ""; // Client secret
        public const string Issuer = ""; // Token issuer
        public const string SampleApiScope = ""; // Scope name of the sample API known by authority
    }
}
